kind: Service
apiVersion: v1
metadata:
  name:  nginx
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
  - protocol: TCP
    name:  nginxport
    port:  80
    targetPort: 80
    nodePort: 30868